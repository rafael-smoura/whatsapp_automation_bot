# whatsapp_automation_bot
This project is a WhatsApp automation bot built with Python + Selenium, integrated with a PHP backend and MySQL, running locally via XAMPP.  The bot monitors unread messages on WhatsApp Web,captures incoming messages, sends them to a local PHP endpoint for processing, and automatically replies to the user,all without using the official WhatsApp API
